codeunit 50113 "WLD DecoupledStuff"
{
/*     [EventSubscriber(ObjectType::Codeunit, Codeunit::"CreatePallet Meth", 'OnBeforeCreatePallet', '', false, false)]
    local procedure NewCreatePalletMethod(var Item: Record Item;var Handled: Boolean)
    begin
        //message('New method to handle create pallet for item %1', item."No.");

        Handled := true;
    end; */
    
}